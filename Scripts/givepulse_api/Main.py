# Script that will call of the api requests we need (still need to figure out what exactly those are)
import Authorize

# Execution Starts Here
# First need to get an authorization token
token = Authorize.getAuthToken()

# Make other calls here and pass the token as a parameter
